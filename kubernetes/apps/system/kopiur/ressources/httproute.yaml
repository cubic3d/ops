apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kopiur-nas-ui
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
  hostnames:
    - kopiur-nas.${domain}
  rules:
    - backendRefs:
        - name: nas-kopia-ui
          port: 51515
