# yaml-language-server: $schema=https://schemaz.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kopia
spec:
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    template:
      data:
        repository.config.nas: |-
          {
            "storage": {
              "type": "s3",
              "config": {
                "bucket": "kopia",
                "endpoint": "nas.${domain}:3000",
                "accessKeyID": "{{ .nas_s3_kopia_access_key }}",
                "secretAccessKey": "{{ .nas_s3_kopia_secret_key }}"
              }
            },
            "hostname": "kopia.system.svc.cluster.local",
            "username": "kopia",
            "description": "Kopia NAS",
            "enableActions": false
          }
        repository.config.cloud: |-
          {
            "storage": {
              "type": "s3",
              "config": {
                "bucket": "kopia",
                "endpoint": "{{ .e2_endpoint }}",
                "accessKeyID": "{{ .e2_kopia_access_key }}",
                "secretAccessKey": "{{ .e2_kopia_secret_key }}"
              }
            },
            "hostname": "kopia.system.svc.cluster.local",
            "username": "kopia",
            "description": "Kopia S3 Cloud",
            "enableActions": false
          }
        repo_key: "{{ .volsync_kopia_password }}"

  dataFrom:
    - extract:
        key: volsync-template
      rewrite:
        - regexp:
            source: "(.*)"
            target: "volsync_$1"

    - extract:
        key: nas
      rewrite:
        - regexp:
            source: "(.*)"
            target: "nas_$1"

    - extract:
        key: e2
      rewrite:
        - regexp:
            source: "(.*)"
            target: "e2_$1"
