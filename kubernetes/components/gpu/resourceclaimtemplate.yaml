apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: ${app}-gpu
spec:
  spec:
    devices:
      requests:
        - name: gpu
          exactly:
            deviceClassName: gpu.intel.com
            adminAccess: true
            allocationMode: All
