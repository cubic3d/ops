# yaml-language-server: $schema=https://raw.githubusercontent.com/fluxcd-community/flux2-schemas/main/helmrelease-helm-v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: s3-gateway
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 3.6.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system

  values:
    controllers:
      s3-gateway:
        strategy: RollingUpdate

        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          app:
            image:
              # TODO: Revert to official when https://github.com/rclone/rclone/issues/8277 is fixed
              repository: docker.io/cubic3d/rclone
              tag: test

            args:
              - serve
              - s3
              - "box:"

            env:
              RCLONE_ADDR: :443
              RCLONE_CONFIG: /config/rclone.conf
              RCLONE_CERT: /cert/tls.crt
              RCLONE_KEY: /cert/tls.key

            envFrom:
              - secretRef:
                  name: s3-gateway

            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL

            resources:
              requests:
                cpu: 300m
                memory: 50Mi
              limits:
                memory: 512Mi

    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 2000
        runAsGroup: 2000
        fsGroup: 2000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault

    service:
      app:
        controller: s3-gateway
        ports:
          http:
            enabled: false
            port: 80
          https:
            primary: true
            port: 443

    persistence:
      config:
        type: emptyDir
      cert:
        type: secret
        name: s3-gateway-tls
