apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Alert
metadata:
  name: status
spec:
  providerRef:
    name: github
  eventSeverity: info
  eventSources:
    - kind: Kustomization
      name: cluster
      namespace: flux-system
  exclusionList:
    - "waiting.*socket"
